/*
 * Caches container metricsets. Does not (currently) eventify.
 */
export default function container (params) {
  if (params.settings.cache) {
    // Default caching
    //params.tools.cache.metricset(params.data.system.load, params)
  }
}
