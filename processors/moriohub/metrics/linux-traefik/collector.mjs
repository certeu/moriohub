/*
 * Caches collector metricset. Does not (currently) eventify.
 */
export default function collector (params) {
  if (params.settings.cache) {
    // Default caching
    //params.tools.cache.metricset(params.data.system.load, params)
  }
}
