- moriodata:
    info: "Collects log data from Elasticsearch on Linux systems"
    vars:
      LOGS_LINUX_ELASTICSEARCH_AUDIT_LOG_PATHS:
        dflt:
          - /var/log/elasticsearch/*_audit.json
        info: The path(s) to the Elasticsearch audit logs

      LOGS_LINUX_ELASTICSEARCH_DEPRECATION_LOG_PATHS:
        dflt:
          - /var/log/elasticsearch/*_deprecation.json
        info: The path(s) to the Elasticsearch deprecation logs

      LOGS_LINUX_ELASTICSEARCH_ESQL_LOG_PATHS:
        dflt:
          - /var/log/elasticsearch/*_esql.json
        info: The path(s) to the Elasticsearch esql logs

      LOGS_LINUX_ELASTICSEARCH_GC_LOG_PATHS:
        dflt:
          - /var/log/elasticsearch/gc.log
        info: The path(s) to the Elasticsearch garbage collector logs

      LOGS_LINUX_ELASTICSEARCH_SERVER_LOG_PATHS:
        dflt:
          - /var/log/elasticsearch/*_server.json
        info: The path(s) to the Elasticsearch server logs

      LOGS_LINUX_ELASTICSEARCH_SLOW_SEARCH_LOG_PATHS:
        dflt:
          - /var/log/elasticsearch/*_index_search_slowlog.json
        info: The path(s) to the Elasticsearch slow search logs

      LOGS_LINUX_ELASTICSEARCH_SLOW_INDEX_LOG_PATHS:
        dflt:
          - /var/log/elasticsearch/*_index_indexing_slowlog.json
        info: The path(s) to the Elasticsearch slow indexing logs

- type: filestream
  id: elasticsearch_audit_log
  paths: {|{ LOGS_LINUX_ELASTICSEARCH_AUDIT_LOG_PATHS }|}
  processors:
    # Set dataset
    - add_labels:
        labels:
          morio.dataset: audit
    # Parse log message as JSON
    - decode_json_fields:
        fields: ["message"]
        process_array: false
        max_depth: 2
        target: "elasticsearch.audit.log"
        overwrite_keys: false
        add_error_key: false

- type: filestream
  id: elasticsearch_deprecation_log
  paths: {|{ LOGS_LINUX_ELASTICSEARCH_DEPRECATION_LOG_PATHS }|}
  processors:
    # Set dataset
    - add_labels:
        labels:
          morio.dataset: deprecation
    # Parse log message as JSON
    - decode_json_fields:
        fields: ["message"]
        process_array: false
        max_depth: 2
        target: "elasticsearch.deprecation.log"
        overwrite_keys: false
        add_error_key: false

- type: filestream
  id: elasticsearch_esql_log
  paths: {|{ LOGS_LINUX_ELASTICSEARCH_ESQL_LOG_PATHS }|}
  processors:
    # Set dataset
    - add_labels:
        labels:
          morio.dataset: esql
    # Parse log message as JSON
    - decode_json_fields:
        fields: ["message"]
        process_array: false
        max_depth: 2
        target: "elasticsearch.esql.log"
        overwrite_keys: false
        add_error_key: false

- type: filestream
  id: elasticsearch_gc_log
  paths: {|{ LOGS_LINUX_ELASTICSEARCH_GC_LOG_PATHS }|}
  processors:
    # Set dataset
    - add_labels:
        labels:
          morio.dataset: gc

- type: filestream
  id: elasticsearch_server_log
  paths: {|{ LOGS_LINUX_ELASTICSEARCH_SERVER_LOG_PATHS }|}
  processors:
    # Set dataset
    - add_labels:
        labels:
          morio.dataset: server
    # Parse log message as JSON
    - decode_json_fields:
        fields: ["message"]
        process_array: false
        max_depth: 2
        target: "elasticsearch.server.log"
        overwrite_keys: false
        add_error_key: false

- type: filestream
  id: elasticsearch_slow_index_log
  paths: {|{ LOGS_LINUX_ELASTICSEARCH_SLOW_INDEX_LOG_PATHS }|}
  processors:
    # Set dataset
    - add_labels:
        labels:
          morio.dataset: slow_index
    # Parse log message as JSON
    - decode_json_fields:
        fields: ["message"]
        process_array: false
        max_depth: 2
        target: "elasticsearch.slow.index.log"
        overwrite_keys: false
        add_error_key: false

- type: filestream
  id: elasticsearch_slow_search_log
  paths: {|{ LOGS_LINUX_ELASTICSEARCH_SLOW_SEARCH_LOG_PATHS }|}
  processors:
    # Set dataset
    - add_labels:
        labels:
          morio.dataset: slow_search
    # Parse log message as JSON
    - decode_json_fields:
        fields: ["message"]
        process_array: false
        max_depth: 2
        target: "elasticsearch.slow.search.log"
        overwrite_keys: false
        add_error_key: false
